[Unit]
# Fix: Regolith 3.4 session doesn't pull in gnome-session-x11.target,
# so gsd-xsettings never starts and GTK3 apps lose theming.
Requires=gnome-session-x11.target
BindsTo=gnome-session-x11.target
After=gnome-session-x11.target
